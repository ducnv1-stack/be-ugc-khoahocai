/**
 * Cấu hình các tính năng liên quan đến việc xóa dữ liệu.
 * Bạn có thể thay đổi các giá trị ở đây để bật/tắt nhanh các tính năng mà không cần sửa logic phức tạp.
 */
export const DELETE_CONFIG = {
  // Bật/tắt toàn bộ tính năng xóa của Admin (bao gồm cả xóa mềm và xóa vĩnh viễn)
  // Sửa thành false nếu muốn vô hiệu hóa ngay lập tức.
  ALLOW_ADMIN_DELETE: true,
  
  // Các cấu hình bổ sung trong tương lai có thể thêm vào đây
};
